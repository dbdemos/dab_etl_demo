# This is a Databricks asset bundle definition for dab_etl_demo.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dab_etl_demo

include:
  - resources/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-6959653494322738.18.azuredatabricks.net

  prod:
    mode: production
    workspace:
      host: https://adb-323392870981458.18.azuredatabricks.net
      # We explicitly specify /Workspace/Users/suchith.nair@databricks.com to make sure we only have a single copy.
      root_path: /Workspace/Users/suchith.nair@databricks.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: suchith.nair@databricks.com
        level: CAN_MANAGE
    run_as:
      user_name: suchith.nair@databricks.com
